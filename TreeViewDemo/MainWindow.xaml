<Window
    x:Class="TreeViewDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TreeViewDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Path=WindowTitel, FallbackValue=WindowTitel}"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/Resources/Picture/ApplicationIcon.png" />
    </Window.Icon>
    <Grid x:Name="gridMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <WrapPanel
            Grid.Row="0"
            Grid.Column="0"
            Orientation="Horizontal">
            <Button
                x:Name="BtnCloseApplication"
                Width="30"
                Height="30"
                HorizontalAlignment="Left">
                <Image Source="{StaticResource ResourceKey=IconApplicationExit}" Stretch="None" />
            </Button>

            <Button
                x:Name="BtnBeispielA"
                Width="150"
                Height="30"
                Margin="5,0,0,0"
                HorizontalAlignment="Left">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource ResourceKey=IconTreeViewStatic}" Stretch="None" />
                        <TextBlock
                            Margin="5,0,0,0"
                            FontSize="14"
                            FontWeight="Medium"
                            Text="TreeView statisch" />
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button
                x:Name="BtnBeispielB"
                Width="160"
                Height="30"
                Margin="5,0,0,0"
                HorizontalAlignment="Left">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource ResourceKey=IconTreeViewStatic}" Stretch="None" />
                        <TextBlock
                            Margin="5,0,0,0"
                            FontSize="14"
                            FontWeight="Medium"
                            Text="TreeView dynamisch" />
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button
                x:Name="BtnBeispielC"
                Width="170"
                Height="30"
                Margin="5,0,0,0"
                HorizontalAlignment="Left">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource ResourceKey=IconTreeViewStatic}" Stretch="None" />
                        <TextBlock
                            Margin="5,0,0,0"
                            FontSize="14"
                            FontWeight="Medium"
                            Text="TreeView NodeModel" />
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button
                x:Name="BtnBeispielD"
                Width="170"
                Height="30"
                Margin="5,0,0,0"
                HorizontalAlignment="Left">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource ResourceKey=IconTreeViewStatic}" Stretch="None" />
                        <TextBlock
                            Margin="5,0,0,0"
                            FontSize="14"
                            FontWeight="Medium"
                            Text="TreeView Listen" />
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button
                x:Name="BtnBeispielE"
                Width="170"
                Height="30"
                Margin="5,10,0,0"
                HorizontalAlignment="Left">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource ResourceKey=IconTreeViewStatic}" Stretch="None" />
                        <TextBlock
                            Margin="5,0,0,0"
                            FontSize="14"
                            FontWeight="Medium"
                            Text="TreeView Rekursiv" />
                    </StackPanel>
                </Button.Content>
            </Button>
        </WrapPanel>

        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="24"
            Text="Verschiedene Lösungen zum TreeView in WPF." />

    </Grid>
</Window>
